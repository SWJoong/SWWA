# 보안 정책 (Security Policy)

SWWA(swwa-mcp) 서버의 보안 취약점을 책임감 있게 신고하는 방법을 안내합니다.

## 지원 버전

| 버전 | 지원 |
|---|---|
| 0.1.x | ✅ (초기 개발판) |

첫 공개 릴리스(`v0.1.0`) 전까지는 최신 커밋을 기준으로 대응합니다.

## 취약점 신고 방법

**공개 이슈로 올리지 마세요.** 보안 문제는 비공개로 알려 주세요.

1. 이 저장소의 **Security 탭 → "Report a vulnerability"**(GitHub 비공개 보안 신고)를 이용해 주세요.
2. 다음 내용을 적어 주시면 빠르게 확인할 수 있습니다.
   - 어떤 문제인지, 어떻게 재현하는지
   - 영향(무엇이 가능해지는지)
   - 사용한 버전과 실행 환경(OS, Node.js 버전)

접수 후 **7일 안에** 확인 회신을 드리고, 수정 방향과 일정을 함께 알려 드립니다.

## 이 서버의 보안 특성 (공격 표면이 작은 이유)

- **판정·측정은 서버, 해석은 클라이언트 LLM**(ADR-01). 서버는 API 키 없이 결정적으로 동작한다.
- **입력을 저장·전송·로깅하지 않습니다**(NFR-03). 표준 오류(stderr) 로그에도 입력 HTML·URL의 쿼리·헤더 값·페이지 텍스트를 남기지 않는다. `outputDir`를 지정했을 때만 리포트·스크린샷을 파일로 저장한다.
- **입력 크기에 상한**이 있다. `check_html`은 2,000,000자, HTML 파일은 2MB로 제한하고 `maxFindings`로 응답 크기도 절단한다.
- **URL 감사(`audit_url`)는 안전장치가 있다**: http/https/file 스킴만 허용, 링크로컬·클라우드 메타데이터 호스트(`169.254.0.0/16`·`metadata.google.internal` 등)는 차단한다(NFR-07).
- **데이터는 패키지 번들 정적 JSON/MD**이며 런타임 fetch를 하지 않는다(ADR-08).
- **런타임 의존성은 검사 엔진에 필요한 것만** 유지한다(`@modelcontextprotocol/sdk`, `zod`, `axe-core`, `@axe-core/playwright`, `jsdom`, `playwright-core`, `parse5`). 잠금 파일(`package-lock.json`)을 커밋하고 `npm audit`를 CI에서 확인한다.

## 범위

이 서버의 자동 판정은 **전문가·사용자 심사 전 참고 결과**입니다. 인증 심사를 대체하지 않습니다. 규칙의 오탐·미탐은 보안 문제가 아니라 품질 이슈이며, [오탐·미탐 신고 템플릿](.github/ISSUE_TEMPLATE/false-detection.yml)으로 알려 주세요.
